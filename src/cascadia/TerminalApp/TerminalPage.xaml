<!--
    Copyright (c) Microsoft Corporation. All rights reserved. Licensed under
    the MIT License. See LICENSE in the project root for license information.
-->
<Page x:Class="TerminalApp.TerminalPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:TerminalApp"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:mtu="using:Microsoft.Terminal.UI"
      xmlns:mux="using:Microsoft.UI.Xaml.Controls"
      Background="Transparent"
      mc:Ignorable="d">

    <Grid x:Name="Root"
          Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="LeftSidebarColumn" Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition x:Name="RightSidebarColumn" Width="0" />
        </Grid.ColumnDefinitions>

        <!-- Vertical Tab Sidebar -->
        <Grid x:Name="VerticalTabSidebar"
              Grid.Column="0"
              MinWidth="48"
              Width="200"
              MaxWidth="400"
              Background="{ThemeResource TabViewBackground}"
              KeyDown="_KeyDownHandler">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ListView x:Name="VerticalTabListView"
                      Grid.Row="0"
                      Padding="4"
                      SelectionMode="Single"
                      SelectionChanged="_OnVerticalTabSelectionChanged">
                <!-- 关掉 ListViewItemPresenter 内置的选中/悬停/按下高亮。
                     Presenter 选中矩形的 Margin/CornerRadius 是硬编码的,无法靠
                     ListViewItem 的 Padding/CornerRadius 消除留白;而 SelectionBackground
                     等是 Presenter 的属性(不是 ListViewItem 的,不能在 Style 里 Setter)。
                     这里在 ListView 局部 Resources 里把驱动这些高亮的 ThemeResource 覆盖成
                     透明(就近原则,只影响本 ListView,不污染 CommandPalette/Settings 等其他
                     ListView)。选中态视觉改由 bgBorder(整行铺满、Margin=0)承担。 -->
                <ListView.Resources>
                    <ResourceDictionary>
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelected" Color="Transparent" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver" Color="Transparent" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPressed" Color="Transparent" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="Transparent" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundPressed" Color="Transparent" />
                    </ResourceDictionary>
                </ListView.Resources>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <!-- VerticalContentAlignment=Stretch 让内容(grid)撑满
                             ListViewItem 的整行高度;否则 grid 垂直居中只占内容高度,
                             内部 bgBorder 即使 Stretch 也只撑满矮矮的内容区,
                             上下露出留白。 -->
                        <Setter Property="VerticalContentAlignment" Value="Stretch" />
                        <!-- Padding=0 让相邻标签上下紧挨、彻底无缝(配合 bgBorder 的
                             Margin=0,标签色平铺到容器四边,无外露背景边)。 -->
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="MinHeight" Value="32" />
                        <!-- 自定义 ControlTemplate:只用一个撑满的 Border + ContentPresenter,
                             不用 ListViewItemPresenter。默认模板的 Presenter 在选中/悬停时会
                             画自带 Margin+圆角的高亮矩形(蓝色选中框),无法靠 ThemeResource
                             覆盖或 Padding 消除(实测局部 Resources 覆盖不生效,蓝色仍在)。
                             去掉 Presenter 后,选中态无任何默认背景,橙色 bgBorder(整行铺满)
                             成为唯一可见的选中/颜色背景。模板内不声明 Selected 视觉状态画背景。 -->
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter Content="{TemplateBinding Content}"
                                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>

            <Button x:Name="VerticalNewTabButton"
                    Grid.Row="1"
                    Margin="4,4,4,2"
                    Padding="8,6"
                    HorizontalAlignment="Stretch"
                    Click="_OnVerticalNewTabButtonClick"
                    Content="&#xE710;"
                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                    FontSize="12" />

            <Button x:Name="VerticalSettingsButton"
                    Grid.Row="2"
                    Margin="4,2,4,2"
                    Padding="8,6"
                    HorizontalAlignment="Stretch"
                    Content="&#xE713;"
                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                    FontSize="12" />

            <!-- Workspace button: only visible in vertical (left/right) mode.
                 Reuses the same _workspaceFlyout as the upstream TabRow
                 WorkspaceDropdown (shown programmatically via ShowAt in
                 _OnWorkspaceButtonClick). Icon EE40 matches upstream. -->
            <Button x:Name="VerticalWorkspaceButton"
                    Grid.Row="3"
                    Margin="4,2,4,8"
                    Padding="8,6"
                    HorizontalAlignment="Stretch"
                    Click="_OnWorkspaceButtonClick"
                    Content="&#xEE40;"
                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                    FontSize="12"
                    Visibility="Collapsed" />
        </Grid>

        <!-- Resize grip between sidebar and content -->
        <Border x:Name="SidebarResizeGrip"
                Grid.Column="0"
                Width="4"
                HorizontalAlignment="Right"
                Background="Transparent"
                PointerEntered="_OnSidebarResizePointerEntered"
                PointerExited="_OnSidebarResizePointerExited"
                PointerPressed="_OnSidebarResizePointerPressed"
                PointerMoved="_OnSidebarResizePointerMoved"
                PointerReleased="_OnSidebarResizePointerReleased" />

        <!-- Main content area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Horizontal TabRow - visible in top mode, hidden in left/right mode -->
            <local:TabRowControl x:Name="TabRow"
                                 Grid.Row="0"
                                 KeyUp="_KeyDownHandler" />

            <StackPanel Grid.Row="1"
                        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <mux:InfoBar x:Name="KeyboardServiceWarningInfoBar"
                             x:Load="False"
                             CornerRadius="0"
                             IsClosable="True"
                             IsIconVisible="True"
                             IsOpen="False"
                             Message="{x:Bind KeyboardServiceDisabledText, Mode=OneWay}"
                             Severity="Warning">
                    <mux:InfoBar.ActionButton>
                        <Button x:Uid="InfoBarDismissButton"
                                Click="_KeyboardServiceWarningInfoDismissHandler" />
                    </mux:InfoBar.ActionButton>
                </mux:InfoBar>

                <mux:InfoBar x:Name="CloseOnExitInfoBar"
                             x:Uid="CloseOnExitInfoBar"
                             x:Load="False"
                             CornerRadius="0"
                             IsClosable="True"
                             IsIconVisible="True"
                             IsOpen="False"
                             Severity="Informational">
                    <mux:InfoBar.ActionButton>
                        <Button x:Uid="InfoBarDismissButton"
                                Click="_CloseOnExitInfoDismissHandler" />
                    </mux:InfoBar.ActionButton>
                </mux:InfoBar>
            </StackPanel>

            <Grid x:Name="TabContent"
                  Grid.Row="2"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch">
                <Grid.Background>
                    <ImageBrush />
                </Grid.Background>
            </Grid>

            <!--
                GH#12775 et. al: Assigning all the dialogs to Row 2 (where the
                rest of the TabContent is) makes the "hole" appear in the same
                space as the rest of the TabContent, fixing the issue.
            -->

            <local:AboutDialog x:Name="AboutDialog"
                               Grid.Row="2"
                               x:Load="False" />

            <ContentDialog x:Name="ConfirmCloseDialog"
                           Grid.Row="2"
                           x:Load="False"
                           DefaultButton="Primary">
                <CheckBox x:Uid="DontAskAgainCheckBox" />
            </ContentDialog>

            <ContentDialog x:Name="CloseReadOnlyDialog"
                           x:Uid="CloseReadOnlyDialog"
                           Grid.Row="2"
                           x:Load="False"
                           DefaultButton="Close" />

            <ContentDialog x:Name="MultiLinePasteDialog"
                           x:Uid="MultiLinePasteDialog"
                           Grid.Row="2"
                           x:Load="False"
                           DefaultButton="Primary">
                <StackPanel>
                    <TextBlock x:Uid="MultiLineWarningText"
                               TextWrapping="Wrap" />
                    <TextBlock x:Uid="ClipboardTextHeader"
                               Margin="0,16,0,0" />
                    <ScrollViewer x:Name="ClipboardContentScrollViewer"
                                  MaxHeight="100"
                                  Margin="0,8,0,0">
                        <TextBlock x:Name="ClipboardText"
                                   FontFamily="Cascadia Mono"
                                   TextWrapping="Wrap" />
                    </ScrollViewer>
                </StackPanel>
            </ContentDialog>

            <ContentDialog x:Name="LargePasteDialog"
                           x:Uid="LargePasteDialog"
                           Grid.Row="2"
                           x:Load="False"
                           DefaultButton="Primary" />

            <ContentDialog x:Name="ControlNoticeDialog"
                           x:Uid="ControlNoticeDialog"
                           Grid.Row="2"
                           x:Load="False"
                           DefaultButton="Primary">
                <TextBlock IsTextSelectionEnabled="True"
                           TextWrapping="WrapWholeWords">
                    <TextBlock.ContextFlyout>
                        <mtu:TextMenuFlyout />
                    </TextBlock.ContextFlyout>
                    <Run x:Name="NoticeMessage" />
                </TextBlock>
            </ContentDialog>

            <ContentDialog x:Name="UriErrorDialog"
                           x:Uid="UriErrorDialog"
                           Grid.Row="2"
                           x:Load="False"
                           DefaultButton="Close">
                <TextBlock IsTextSelectionEnabled="True"
                           TextWrapping="WrapWholeWords">
                    <TextBlock.ContextFlyout>
                        <mtu:TextMenuFlyout />
                    </TextBlock.ContextFlyout>
                    <Run x:Name="CouldNotOpenUriReason" /> <LineBreak /> <LineBreak />
                    <Run x:Name="UnopenedUri"
                         FontFamily="Cascadia Mono" />
                </TextBlock>
            </ContentDialog>

            <local:CommandPalette x:Name="CommandPaletteElement"
                                  Grid.Row="2"
                                  VerticalAlignment="Stretch"
                                  x:Load="False"
                                  PreviewKeyDown="_KeyDownHandler"
                                  Visibility="Collapsed" />

            <local:SuggestionsControl x:Name="SuggestionsElement"
                                      Grid.Row="2"
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Top"
                                      x:Load="False"
                                      PreviewKeyDown="_KeyDownHandler"
                                      Visibility="Collapsed" />

            <mux:TeachingTip x:Name="WindowIdToast"
                             Title="{x:Bind WindowProperties.WindowIdForDisplay}"
                             x:Load="False"
                             IsLightDismissEnabled="True"
                             Subtitle="{x:Bind WindowProperties.WindowNameForDisplay, Mode=OneWay}" />

            <mux:TeachingTip x:Name="WindowRenamer"
                             x:Uid="WindowRenamer"
                             Title="{x:Bind WindowProperties.WindowIdForDisplay}"
                             x:Load="False"
                             ActionButtonClick="_WindowRenamerActionClick"
                             ActionButtonStyle="{ThemeResource AccentButtonStyle}"
                             IsLightDismissEnabled="True">
                <mux:TeachingTip.Content>
                    <TextBox x:Name="WindowRenamerTextBox"
                             KeyDown="_WindowRenamerKeyDown"
                             KeyUp="_WindowRenamerKeyUp"
                             Text="{x:Bind WindowProperties.WindowName, Mode=OneWay}">
                        <TextBox.ContextFlyout>
                            <mtu:TextMenuFlyout />
                        </TextBox.ContextFlyout>
                    </TextBox>
                </mux:TeachingTip.Content>
            </mux:TeachingTip>

            <mux:TeachingTip x:Name="WindowCwdToast"
                             x:Uid="WindowCwdToast"
                             Title="{x:Bind WindowProperties.VirtualWorkingDirectory, Mode=OneWay}"
                             x:Load="False"
                             IsLightDismissEnabled="True" />

            <mux:TeachingTip x:Name="ActionSavedToast"
                             x:Uid="ActionSavedToast"
                             Title="Action Saved"
                             HorizontalAlignment="Stretch"
                             x:Load="False"
                             IsLightDismissEnabled="True">
                <mux:TeachingTip.Content>
                    <StackPanel HorizontalAlignment="Stretch"
                                Orientation="Vertical">
                        <TextBlock x:Name="ActionSavedNameText"
                                   Visibility="{x:Bind mtu:Converters.StringNotEmptyToVisibility(SavedActionName), Mode=OneWay}">
                            <Run Text="Name: " />
                            <Run Text="{x:Bind SavedActionName, Mode=OneWay}" />
                        </TextBlock>
                        <TextBlock x:Name="ActionSavedKeyChordText"
                                   Visibility="{x:Bind mtu:Converters.StringNotEmptyToVisibility(SavedActionKeyChord), Mode=OneWay}">
                            <Run Text="Key Chord: " />
                            <Run Text="{x:Bind SavedActionKeyChord, Mode=OneWay}" />
                        </TextBlock>
                        <TextBlock x:Name="ActionSavedCommandLineText"
                                   Visibility="{x:Bind mtu:Converters.StringNotEmptyToVisibility(SavedActionCommandLine), Mode=OneWay}">
                            <Run Text="Input: " />
                            <Run Text="{x:Bind SavedActionCommandLine, Mode=OneWay}" />
                        </TextBlock>
                    </StackPanel>
                </mux:TeachingTip.Content>
            </mux:TeachingTip>
            <mux:TeachingTip x:Name="ActionSaveFailedToast"
                             x:Uid="ActionSaveFailedToast"
                             Title="Action Save Failed"
                             x:Load="False"
                             IsLightDismissEnabled="True">
                <mux:TeachingTip.Content>
                    <TextBlock x:Name="ActionSaveFailedMessage"
                               Text="" />
                </mux:TeachingTip.Content>
            </mux:TeachingTip>
        </Grid>
    </Grid>
</Page>
